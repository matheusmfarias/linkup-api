const express = require('express');
const router = express.Router();

// Defina suas rotas de usuário aqui
router.get('/profile', (req, res) => {
  // lógica de perfil de usuário
});

module.exports = router;
